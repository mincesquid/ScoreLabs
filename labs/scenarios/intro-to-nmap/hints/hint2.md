# Hint 2

To identify service versions, you need to use Nmap's version detection feature.

The `-sV` flag will probe open ports and attempt to determine what service and version is running.

Focus your scan on the web server at 10.0.100.10, specifically ports 80 and 443.
